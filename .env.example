# Proxy-WASM Test Runner - Environment Configuration
# Copy this file to .env, .env.secrets, or .env.variables and fill in your values

# ============================================================================
# Option 1: Single .env file with prefixes
# ============================================================================
# Secrets (accessed via proxy_get_secret in WASM)
FASTEDGE_VAR_SECRET_JWT_SECRET=your-jwt-secret-here
FASTEDGE_VAR_SECRET_API_KEY=your-api-key-here
FASTEDGE_VAR_SECRET_DATABASE_PASSWORD=your-db-password-here

# Dictionary/Configuration (accessed via proxy_dictionary_get in WASM)
FASTEDGE_VAR_ENV_API_URL=https://api.example.com
FASTEDGE_VAR_ENV_LOG_LEVEL=debug
FASTEDGE_VAR_ENV_FEATURE_FLAG_NEW_UI=true
FASTEDGE_VAR_ENV_CACHE_TTL=3600

# ============================================================================
# Option 2: Separate files (recommended for better organization)
# ============================================================================
# Create .env.secrets (no prefix needed):
#   JWT_SECRET=your-jwt-secret-here
#   API_KEY=your-api-key-here
#   DATABASE_PASSWORD=your-db-password-here
#
# Create .env.variables (no prefix needed):
#   API_URL=https://api.example.com
#   LOG_LEVEL=debug
#   FEATURE_FLAG_NEW_UI=true
#   CACHE_TTL=3600

# ============================================================================
# Usage
# ============================================================================
# 1. Enable "Load .env files" toggle in Server Properties panel (ON by default)
# 2. Load your WASM binary - secrets and dictionary will be loaded automatically
# 3. Your WASM code can access:
#    - Secrets: Host.getProperty("secret.JWT_SECRET")
#    - Dictionary: Host.getProperty("dictionary.API_URL")
#
# For AI agents: Add "dotenvEnabled": true/false to API requests
#   POST /api/load { "wasmBase64": "...", "dotenvEnabled": true }

# ============================================================================
# Security
# ============================================================================
# IMPORTANT: Add these files to .gitignore:
#   .env
#   .env.*
#   !.env.example
